import React from 'react';
import ReactDOM from "react-dom/client";
import App from './app/App.js';  // Added .js extension
import './index.css';

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
    <App />
);
