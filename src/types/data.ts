export interface Meta {
    title: string;
    description: string;
}

export interface IntroData {
    title: string;
    animated: {
        first: string;
        second: string;
        third: string;
    };
    description: string;
    your_img_url: string;
}
